export enum AppType {
  Terminal = "TERMINAL",
  TextEditor = "TEXTEDITOR",
}

export enum SoundType {
  WindowOpen = "WindowOpen",
  WindowClose = "WindowClose",
  MenuOpen = "MenuOpen",
  Volume = "Volume",
}
